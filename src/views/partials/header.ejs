<header class="header">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="/">Food Recipe Project</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/recipes"><%= t('recipes') %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/nutrition"><%= t('nutrition') %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dishes"><%= t('dishes') %></a>
                    </li>
                    <% if (login) { %>
                        <li class="nav-item">
                            <form action="/logout" method="POST">
                                <button type="submit" class="btn btn-link nav-link"><%= t('logout') %></button>
                            </form>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login"><%= t('login') %></a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register"><%= t('register') %></a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
</header>
